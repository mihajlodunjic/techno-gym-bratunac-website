<div class="gym-map-controls" aria-label="Kontrole uvećanja plana">
  <button type="button" data-gym-map-zoom-out aria-label="Smanji plan">−</button>
  <output data-gym-map-zoom-label aria-live="polite">160%</output>
  <button type="button" data-gym-map-zoom-in aria-label="Uvećaj plan">+</button>
  <button type="button" data-gym-map-reset>Cijeli plan</button>
</div>

<style>
  .gym-map-controls {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.4rem;
  }

  .gym-map-controls button,
  .gym-map-controls output {
    min-width: 2.75rem;
    min-height: 2.75rem;
    border: 1px solid var(--color-ink);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--color-paper-strong);
    color: var(--color-ink);
    font: inherit;
    font-weight: 800;
  }

  .gym-map-controls output {
    padding-inline: 0.65rem;
    font-family: var(--font-display);
    font-size: 1.1rem;
  }

  .gym-map-controls button {
    cursor: pointer;
    transition:
      background-color var(--transition-fast),
      transform var(--transition-fast);
  }

  .gym-map-controls button:hover {
    background: var(--color-amber-light);
    transform: translateY(-1px);
  }

  .gym-map-controls button[data-gym-map-reset] {
    padding-inline: 0.75rem;
    font-size: 0.9rem;
  }

  @media (min-width: 64rem) and (hover: hover) and (pointer: fine) {
    .gym-map-controls {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gym-map-controls button {
      transition: none;
    }

    .gym-map-controls button:hover {
      transform: none;
    }
  }
</style>
