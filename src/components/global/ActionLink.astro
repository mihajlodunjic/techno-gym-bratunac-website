---
import { getAllowedHref, shouldUseExternalTarget } from "@/utils/linkPolicy";

interface Props {
  href: string;
  label: string;
  variant?: "primary" | "inline";
  external?: boolean;
  ariaLabel?: string;
}

const {
  href,
  label,
  variant = "inline",
  external = false,
  ariaLabel
} = Astro.props;

const allowedHref = getAllowedHref(href);
const useExternalTarget = shouldUseExternalTarget(href, external);
---

<a
  class:list={[variant === "primary" ? "button-link" : "inline-link"]}
  href={allowedHref}
  aria-label={ariaLabel}
  target={useExternalTarget ? "_blank" : undefined}
  rel={useExternalTarget ? "noreferrer" : undefined}
>
  <span>{label}</span>
  <svg
    aria-hidden="true"
    width="14"
    height="14"
    viewBox="0 0 14 14"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
  >
    <path
      d="M2 7H12M12 7L7.5 2.5M12 7L7.5 11.5"
      stroke="currentColor"
      stroke-width="1.4"
      stroke-linecap="square"
      stroke-linejoin="miter"
    ></path>
  </svg>
</a>
